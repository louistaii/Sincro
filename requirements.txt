# Fast preview uses the standard library; Precision requires OR-Tools.
# Primary optimisation is uncapped. Hosting request timeouts are separate;
# configure and load-test them for the intended deployment workload.
ortools>=9.8
