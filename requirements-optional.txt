# The solver core and the web app run on the standard library alone.
#
# Installing this unlocks the exact CP-SAT backend for contract-completion
# optimisation. Reports include the local objective bound and proof status;
# equivalence to the inaccessible organiser validator remains unverified.
# Without it every entry point falls back to the heuristic and says so in
# its `solver` field.
#
#   pip install -r requirements-optional.txt
ortools>=9.8

# gemini_client.py uses only the standard library (urllib), so no extra
# package is required to call the Gemini API. Set the GEMINI_API_KEY
# environment variable to enable natural-language querying.
