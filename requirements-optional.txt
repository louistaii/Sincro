# The solver core and the web app run on the standard library alone.
#
# Installing this unlocks the exact CP-SAT backend, which proves the Section
# 2.5 objective instead of approximating it (public instance: 131.6/50/44.5
# against the heuristic's 316.4/110/229.3). Without it every entry point falls
# back to the heuristic and says so in its `solver` field.
#
#   pip install -r requirements-optional.txt
ortools>=9.8
